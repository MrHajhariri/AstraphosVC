# Plugin API

Status: planned for Phase 12.

Plugins will be capability-scoped extensions. The core will define stable entry points, version negotiation, and explicit permissions for repository reads, worktree writes, hooks, transports, and commands.

No plugin loading is implemented in Phase 1.
