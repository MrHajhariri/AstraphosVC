# Performance Guide

Phase 1 performance priorities are fast initialization and low overhead repository discovery. No micro-optimizations are present.

Future performance work must be measured before optimization. Planned benchmark areas include object lookup, index loading, status detection, diff generation, packfile creation, and remote transfer.
